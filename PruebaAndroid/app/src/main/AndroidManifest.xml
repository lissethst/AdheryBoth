<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pruebaandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Perfil_Paciente"></activity>
        <activity android:name=".Menu_PacienteActivity" />
        <activity android:name=".Lista_PacientesActivity" />
        <activity android:name=".SelecPerfilActivity" />
        <activity android:name=".Menu_MedicoActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SuccesUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>